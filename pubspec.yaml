name: sma_3_2_developers
description: "A Flutter project supporting both online and offline modes."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.7.1
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  flutter_cache_manager: ^3.3.0
  dio: ^5.3.2
  connectivity_plus: ^5.0.1
  ffmpeg_kit_flutter: ^5.1.0  # ✅ Correcto, no uses versiones 'min'


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Reglas de estilo y buenas prácticas

flutter:
  uses-material-design: true
